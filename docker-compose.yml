services:
  postgres:
    image: postgres:17
    container_name: postgres_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: todo_training
    ports:
      - "5432:5432"
    volumes:
      - todo_api:/var/lib/postgresql/data
volumes:
  todo_api:
